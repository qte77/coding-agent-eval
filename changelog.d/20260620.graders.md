### Added

- `SolutionGrader` stub (`src/harness/graders/solution_grader.py`): typed placeholder for diff-overlap vs canonical `expected_solution`; raises `NotImplementedError` (implementation pending). `ValidateGrader` and `ScopeGrader` were already present.
- Grader spec tests (`tests/harness/graders/`): `test_solution_grader.py` with 4 behaviour tests marked `xfail(strict=True)` (TDD red); 2 additional cases added to `test_scope_grader.py` (all-in-scope, none-in-scope — green).
